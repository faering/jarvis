# Secrets
.env
.devcontainer/devcontainer.env
*.local

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/
*.egg-info/

# Node / frontend
node_modules/
dist/
.vite/

# Rust / Tauri
target/
frontend/src-tauri/target/

# OS / editor
.DS_Store
*.swp

# act
.actrc.local

# Local work-item mirror (synced from GitHub; GitHub is source of truth)
github-issues.json

# Personal, per-user settings (shared equivalents: .claude/settings.json, CLAUDE.md,
# and devcontainer.json -> customizations.vscode.settings)
.claude/settings.local.json
CLAUDE.local.md
.vscode/

# Model weights store (large binaries, fetched at runtime); manifest is tracked
models/store/
